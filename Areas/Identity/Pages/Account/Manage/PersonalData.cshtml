@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dados Pessoais";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Sua conta possui dados que você nos cedeu. Essa página permite que você baixe ou delete esses dados.</p>
        <p>
            <strong>Apagar esses dados irá excluir permanentemente sua conta, esse processo não poderá ser desfeito.</strong>
        </p>
        <div class="form-group text-center">
            <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group">
                <button class="btn btn-light p-2" type="submit">Download</button>
            </form>
            <p>
                <a id="delete" asp-page="DeletePersonalData" class="btn btn-light p-2">Apagar</a>
            </p>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}